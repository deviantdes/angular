import { Component } from "@angular/core";

@Component({
    moduleId: module.id,
    selector: "dir-app",
    templateUrl: "directory.component.html"
})

export class DirectoryComponent
{
    message: String = "in directory 1!";
}
   